# Backend

Este diretório contém o servidor que serve a aplicação frontend em modo estático.

## Como rodar

```bash
node backend/server.js
```

A aplicação ficará disponível em http://localhost:3000.
